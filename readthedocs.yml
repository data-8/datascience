version: 2

formats: all

build:
  os: ubuntu-24.04
  tools:
    python: "3.6"

conda:
  environment: linux_environment.yml

python:
  install:
    - requirements: requirements.txt
    - requirements: docs/requirements.txt
    - method: setuptools
      path: .
